services:
  nauti-engine:
    build: .
    ports:
      # Host port 4977 to avoid clashing with local Dapta engine containers on 4096
      - "4977:4096"
    env_file:
      - .env
    volumes:
      - nauti-data:/data

volumes:
  nauti-data:
